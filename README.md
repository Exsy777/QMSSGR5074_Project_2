# QMSSGR5074_Project_2
Chest X-ray classification using CNNs and transfer learning to detect COVID, normal, and pneumonia cases. Compares multiple models under a consistent pipeline. Fine-tuned ResNet50 achieves best performance (~94% accuracy), while augmentation improves generalization for simpler models.
